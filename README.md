# Make React File

## Use

`go install github.com/wbroberts/mrf`

`mrf <command> <arg>`

## Optional Config

`touch make-react-file.yaml`

```yaml
# All values are optional
component:
  dir: src/components
  props: true
  skip-tests: false
```
